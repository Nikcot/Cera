{
    "name": "cera-extension",
    "version": "1.0.0",
    "private": true,
    "description": "Цера — Chrome Extension с Whisper транскрибацией",
    "scripts": {
        "build": "node build.js",
        "watch": "node build.js --watch"
    },
    "dependencies": {
        "@huggingface/transformers": "^3.4.0"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}